{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": [
        "Dockerfile"
      ],
      "matchStrings": [
        "CADDY_VERSION=(?<currentValue>v[0-9\\.]+)"
      ],
      "datasourceTemplate": "go",
      "packageNameTemplate": "github.com/caddyserver/caddy/v2",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "fileMatch": [
        "Dockerfile"
      ],
      "matchStrings": [
        "github\\.com/[a-zA-Z0-9._/-]+@(?<currentValue>v[0-9\\.\\-]+)"
      ],
      "datasourceTemplate": "go",
      "packageNameTemplate": "{{matchString}}",
      "versioningTemplate": "semver"
    }
  ]
}
